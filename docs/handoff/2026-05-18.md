# Handoff — 2026-05-18 [05:00 session start]

## Session Goal
User reported: clicking "Resume session" on a suspended Claude tab "most of the time" produced two duplicate terminal tabs both running `claude --resume` against the SAME UUID. Goal was to diagnose how two tabs were converging on a single session and ship the fix end-to-end (code → build → install).

## What Got Done
Commit `3380f1f` — "fix: per-tab Claude UUID binding to stop suspend/resume convergence":

- **New `claudeUuidByTab` signal** (`src/components/TerminalsView.tsx`): per-tab Claude session UUID, seeded at restore from each `listPersistedTerminals` row's `claude_session_id`, refreshed on every spawn via the post-spawn poller, and cleaned up in `closeTerminal`.
- **Post-spawn jsonl-diff polling** in `handlePaneSpawned`: snapshots the cwd's existing `.jsonl` set BEFORE the 250 ms post-spawn write, then polls at 1.5 s, +2 s, +2.5 s, +4 s (≈ 10 s total) for a UUID present in the cwd but NOT in the snapshot. That UUID is the NEW session `claude` created on resume, and gets bound to this specific tab. Aborts if tab closed or re-suspended mid-poll.
- **Defensive guard against handlePaneSpawned double-fire**: early-return + `console.warn` if `sessionIdByTab[tabId]` is already set. Symptom in the bug report (image 5 — `claude --resume` echoed twice in the same PTY) can only happen if the post-spawn write path fires twice. Guard catches it; the warn lets us trace the cause if it recurs.
- **`suspendTab` reads bound UUID first**: only falls back to `detectClaudeSessionsForCwd(cwd)` when unbound, and the fallback excludes UUIDs already owned by other tabs in the same cwd so two unbound siblings can't grab the same `[0]`. Caches the resolved UUID back into `claudeUuidByTab` for the next cycle.
- **Restore-time dedupe by (cwd, uuid)** in `TerminalsView.onMount`: defensive heal for any stale duplicate rows left in `persisted_terminals` by older builds. Drops them silently with a `console.warn`.
- **`persist_project_terminals` signature change** (`src-tauri/src/commands.rs`): now takes `Vec<PersistTabInput { cwd, claude_session_id }>` instead of `Vec<String>` cwds. Rust uses the per-tab UUIDs directly (no FS rescan), dedupes by (cwd, uuid), and skips any tab whose UUID's `.jsonl` no longer exists via `session_restore::session_file_exists`. The old "scan the cwd, write every UUID we find" implicit-discovery path is gone — same-cwd tabs now persist as N distinct rows, not as M-deduped-by-cwd rows.
- **IPC wrapper updated** (`src/lib/ipc.ts`): new `PersistTabInput` type; `persistProjectTerminals(projectId, tabs)`.

Build path used per `~/.claude/projects/.../memory/feedback_build_install_running_app.md`:
- `cargo tauri build --bundles app` — clean exit, finished in normal time.
- `rm -rf "/Applications/Lexical Emerson.app"` + `ditto "src-tauri/target/release/bundle/macos/Lexical Emerson.app" "/Applications/Lexical Emerson.app"` — verified installed mtime matches build mtime (May 18 06:01:54). Did not `killall` — Claude Code session is hosted inside the running app instance.

## What Was Tried and Abandoned
- **Approach**: Initial guess (from the first round of code reading) that the duplicate-tab symptom was a Solid `<Show>` rendering both branches transiently, or a missed `addTerminal` call somewhere in the resume path.
  - **Why we walked back**: User dropped screenshots showing two tabs both running `claude --resume f2b26e3f-…` with the SAME UUID. That ruled out "second tab created on resume" — both tabs were genuine `allTabs` entries with their own PTYs and post-spawn writes. The convergence was on the UUID, not on the tab count primary. Pivoted from "find the extra setAllTabs call" to "find why two tabs would ever resolve to the same UUID."
  - **Lesson**: Trust user screenshots as the ground truth. When the diagnosis says "this is impossible per the code" and the evidence shows it's happening, the impossibility lives in an unexamined assumption. Here it was "each tab has its own UUID" — actually the suspend path threw that away on every cycle by re-deriving from the cwd scan.
- **Approach**: Considered adding a long-running watcher (every 30 s) for late-typed manual `claude` so binding catches it.
  - **Why we walked back**: Adds polling cost across every tab forever; the user's actual broken case is auto-restore + auto-suspend, which the 10 s post-spawn window covers. Suspend's exclude-list fallback handles late-typed claude correctly within a session. Persist would miss it on close, but that's a smaller class of pain than the original convergence bug.
  - **Lesson**: Ship the minimum fix that handles the reported symptom + the broad class. Reserve the watcher for if the late-typed case actually bites someone.
- **Approach**: Considered making the persist Rust-side fall back to FS detect for tabs with no bound UUID.
  - **Why we walked back**: Re-introduces the cwd-scan that caused convergence. If we want a fallback, it has to be frontend-side with the exclude-list (same logic as suspend's fallback), not Rust-side blind detect. Punted to "if the late-typed case bites, do it then with the right structure."
  - **Lesson**: Don't restore a removed bug pattern in the name of completeness. The whole point of the per-tab signature change was to make Rust trust what the frontend tells it.

## Decisions Made
- **Bind UUID to tab, not to cwd.** Architectural shift — every code path that used to ask "what UUID is in this cwd?" now asks "what UUID has this tab bound?" Captured in STATUS recent-decisions; not promoted to a full ADR because it's a bug fix that aligns with the original intent (tabs were always meant to own their session, the cwd-scan was a shortcut that turned into a bug).
- **Drop implicit "scan cwd at persist, discover orphan sessions" behavior.** Tabs now only persist what they explicitly own. `load_active_claude_sessions_for_bucket` remains the explicit recovery path for orphan-session discovery. Captured in STATUS.

## Open Questions for Next Session
- Did the relaunch + suspend → resume cycle actually work for the user end-to-end? STATUS lists this as the next concrete action — verification is the load-bearing acceptance test for this fix.
- Will the handlePaneSpawned double-fire guard ever trip the warn? If yes, that's the real root cause of the image-5 "command typed twice" symptom and needs a separate fix; if no, the guard is paranoid defense and the symptom was upstream (e.g., the convergence bug feeding a single tab multiple resume commands across cycles).
- Are there any stale rows in production `persisted_terminals` SQLite that the dedupe-on-restore needs to be looking at? Could verify with `sqlite3 ~/Library/Application\ Support/com.mahmutsalman.lexical-emerson/state.db "SELECT project_id, cwd, claude_session_id, COUNT(*) FROM persisted_terminals GROUP BY project_id, cwd, claude_session_id HAVING COUNT(*) > 1"`.
- Worth keeping the `[bind]` and `[persist]` console.info lines, or strip after a week of stable use? They're cheap, leave for now.

## Notes
- Build-install pattern held: rebuilt twice (once for compile-check, once for full bundle), reinstalled once, conversation survived without losing the in-app Claude Code session.
- Diagnostic logging in this commit is intentional: `[bind] tab X → uuid Y`, `[bind] tab X no new UUID after polling`, `[restore] dropping duplicate persisted row …`, `[persist] dedup: skipping repeat …`, `[persist] skipping tab cwd=X uuid=Y — session file gone`, plus the existing `[suspend] …`, `[close] …`, `[persist] enter …`. Filter on `[bind]` and `[persist]` to verify the round-trip per relaunch.
- Pre-existing uncommitted changes (`docs/STATUS.md` — now overwritten by this checkpoint, `docs/handoff/2026-05-17.md`, `src/components/BucketBar.tsx`) were left in the working tree intentionally. Today's commit is scoped to the resume-convergence fix only.
- The TerminalsView fix doesn't touch the bucket workspace's terminal-handling path; that's a separate component with its own restore model. If the bucket workspace ever grows its own suspend/resume, it'll need the same per-tab binding treatment.

---

# Handoff — 2026-05-18 [07:00 session start]

## Session Goal
Complete the keyboard navigation layer: add armed-mode to the terminal/editor tab strip (header), wire vertical arm-switching between header and footer, and fix the ArrowDown focus leak that was sending cursor-down to the Claude Code terminal input.

## What Got Done
Commit `e703bdf` — "feat: armed tab strip with vertical arrow-key switching between header and footer":

- **`cycleAll(delta)` in `TerminalsView.tsx`**: unified `[terminals…, editors…]` list cycling. Switching to an editor calls `editorState.setActive(file.id)`; switching to a terminal clears the editor view, sets the active tab, and auto-resumes if suspended.
- **`tabsArmed` signal + `tabsStripEl` ref**: arms the `.terminal-tabs` div as a focusable keyboard target. `armTabs()` guards: no-ops when total tabs < 2 so the visual cue doesn't appear when there's nothing to cycle.
- **`onTabsKey`**: ArrowLeft/Right → `cycleAll(±1)`; ArrowDown → dispatch `lexical:arm-switch-vertical { target: "footer" }` + `disarmTabs()`; Escape → `disarmTabs()`.
- **`focusActive()` guards**: `if (tabsArmed()) return` prevents xterm stealing focus mid-navigation; `if (verticalSwitching) return` prevents focus during the one-microtask header→footer handoff window.
- **`verticalSwitching` flag**: set to `true` before `disarmTabs()` in the ArrowDown handler, reset via `queueMicrotask`. Also added `e.stopPropagation()` to the ArrowDown branch so the event can't bubble to xterm.
- **`createEffect` on `tabsArmed`**: programmatic focus to strip on arm; on disarm, blurs strip and calls `focusActive()` so user lands immediately on the terminal/editor.
- **`onDocClickForTabs` (capture-phase)**: click anywhere outside the strip disarms it.
- **`onArmSwitch` listener**: registered in `onMount`, removed in `onCleanup`. Calls `armTabs()` when `detail.target === "header"` (dispatched by BucketBar's ArrowUp handler).
- **`.terminal-tabs` JSX**: gets `classList={{ "is-armed": tabsArmed() }}`, `ref={tabsStripEl}`, `tabIndex={-1}`, `onClick={armTabs}`, `onKeyDown={onTabsKey}`, and a `title` hint. All child button `onClick` handlers gain `e.stopPropagation()` to prevent bubbling to the arm action.
- **`BucketBar.tsx` — ArrowUp branch in `onKey`**: dispatches `lexical:arm-switch-vertical { target: "header" }` and calls `broadcastArmed(false)` to clear all sibling windows' footer arms. `onArmSwitch` listener registered in `onMount` / cleaned in `onCleanup` calls existing `arm()` helper when `detail.target === "footer"`.
- **`app.css`**: `.terminal-tabs.is-armed` accent inset top shadow; `.bucket-cycle-btn--prev` `scaleX(-1)` so both BucketBar buttons point outward.

Build path: `cargo tauri build --bundles app` → `rm -rf` + `ditto` per the in-app-hosting rule. Installed mtime: May 18 07:03:33.

## What Was Tried and Abandoned
- **Approach**: Initially planned to use `e.stopPropagation()` alone to block the ArrowDown from reaching xterm after the header→footer handoff.
  - **Why we walked back**: `stopPropagation` blocks DOM bubbling but the real issue was `focusActive()` running asynchronously (microtask + 50 ms setTimeout) AFTER `disarmTabs()` had already cleared the `tabsArmed` guard. Key-repeat ↓ events fired in that async gap and hit the now-focused xterm.
  - **Lesson**: For async focus management, a synchronous guard flag (`verticalSwitching`) that spans the gap is necessary alongside `stopPropagation`.

## Decisions Made
- **`window.dispatchEvent(CustomEvent "lexical:arm-switch-vertical")` for cross-strip coordination.** Both strips live as siblings in the same `App.tsx` window tree — no Tauri round-trip needed. Naming follows project convention (`lexical:timer-open`, `lexical:notes-open-hint`).
- **`verticalSwitching` flag pattern for async focus suppression.** Set synchronously before the disarm; cleared via `queueMicrotask` after the event loop drains. Keeps `focusActive()` safe to call from createEffect without special-casing the call site.

## Open Questions for Next Session
- Is the ↑ direction (footer→header) also susceptible to key-repeat focus leak? BucketBar's ArrowUp calls `broadcastArmed(false)` which is synchronous, and the header's `armTabs()` runs immediately — xterm focus is never offered in that path, so the leak should not occur. Worth a quick manual check under held ↑.
- Cross-window test: ↓ in window A's header should arm BOTH windows' footers (because `broadcastArmed(true)` runs via the receiving `onArmSwitch` in BucketBar). Not explicitly tested this session.

## Notes
- Working tree was clean after commit — no uncommitted changes.
- The `verticalSwitching` flag is a bare `let` variable (not a Solid signal) because it only needs to block a synchronous call path, not trigger reactive updates.
- BucketBar already had the full broadcast infrastructure (`broadcastArmed`, `ARMED_EVENT`, cross-window Tauri listener) from commit `8652c2e` — the vertical switch required only the `onKey` branch addition and the `onArmSwitch` receiver, roughly 6 lines each.

---

# Handoff — 2026-05-18 [session 3]

## Session Goal
Polish the armed keyboard navigation layer: Enter-to-focus-terminal + right-Shift-tap-to-return-to-bar flow; armed header visual cue matching the footer's line; per-bucket configurable idle-suspend timeout.

## What Got Done

**Feature 1 — Enter/right-Shift-tap terminal flow** (uncommitted, staged for this commit):
- `src/lib/arm-focus.ts` (new): Solid signal `lastArmedBar: "footer" | "header"`, default `"footer"`.
- `TerminalsView.tsx`: `armTabs()` sets `lastArmedBar("header")`; `Enter` branch in `onTabsKey` calls `disarmTabs()` (existing createEffect handles terminal refocus); `lexical:focus-terminal` CustomEvent listener in onMount — BucketBar dispatches this on Enter.
- `BucketBar.tsx`: `arm()` sets `lastArmedBar("footer")`; `Enter` branch in `onKey` calls `broadcastArmed(false)` + dispatches `lexical:focus-terminal`; import of `setLastArmedBar`.
- `TerminalPane.tsx`: right-Shift-tap detector — window-level keydown/keyup capture listeners; tap detected if `code === "ShiftRight"`, no other modifier held, no other key fired during hold, released within 300 ms. On clean tap: dispatches `lexical:arm-switch-vertical { target: lastArmedBar() }` (reuses existing arm infrastructure). Listeners registered in onMount, removed in onCleanup. Plain Esc untouched — reaches Claude Code normally.

**Feature 2 — Armed header bottom stripe** (uncommitted):
- `app.css` `.terminal-tabs.is-armed`: extended `box-shadow` from one inset top stripe to two (top + bottom). Bottom stripe (`inset 0 -2px 0 var(--proj-accent, #4f88ff)`) is armed-only signal. Top stripe unchanged (always-on project-identity cue). Root cause of invisible armed state: baseline `.terminal-tabs` already painted the same top stripe — armed/unarmed were visually identical with a project accent set.

**Feature 3 — Per-bucket idle-suspend timeout** (uncommitted):
- `src-tauri/src/store.rs`: `idle_suspend_min INTEGER NOT NULL DEFAULT 60` column via `migrate_buckets_columns()`; Rust `Bucket` struct gains `idle_suspend_min: i64`; `list_buckets`, `get_bucket`, `create_bucket` all updated; new `set_bucket_idle_suspend_min(id, min)` method.
- `src-tauri/src/commands.rs`: `set_bucket_idle_suspend_min` Tauri command; calls `emit_buckets_changed(&app)` so all windows re-fetch.
- `src-tauri/src/main.rs`: command registered in `generate_handler!`.
- `src/lib/types.ts`: `idle_suspend_min: number` added to `Bucket` interface.
- `src/lib/ipc.ts`: `setBucketIdleSuspendMin(id, min)` wrapper.
- `src/components/BucketsList.tsx`: `[15 · 30 · 60]` segmented control inline in bucket header row; `stopPropagation` prevents arm-bucket on button click.
- `src/App.tsx`: `idleSuspendMin = createMemo(() => activeBucket()?.idle_suspend_min ?? 60)` passed to TerminalsView.
- `src/components/TerminalsView.tsx`: `idleSuspendMin?: Accessor<number>` prop; `idleCheckTick` reads `props.idleSuspendMin?.() ?? AUTO_SUSPEND_MIN`.
- `src/styles/app.css`: `.bucket-idle-toggle` and `.bucket-idle-btn` styles.

## What Was Tried and Abandoned
- **Global title-bar idle-timeout toggle**: first design. Abandoned mid-implementation when user redirected to per-bucket assignment. Partially written store.rs methods were reverted before re-planning.
  - **Lesson**: Plan-mode interrupts are cheap — zero-waste pivot because only store.rs had been touched.

## Decisions Made
- **Right-Shift tap (not Esc) for return-to-bar.** Plain Esc would have clobbered Claude Code's own Escape. Right-Shift tap chosen because user's right hand rests next to it. Tap detection (keydown+keyup pair, 300 ms window, taint-on-other-key) distinguishes from Shift-as-modifier.
- **Active bucket governs idle timeout for all project windows.** Multi-bucket project scenario resolved by "active bucket's setting wins" — no per-project override. Switch buckets, timeout changes immediately via `activeBucket()` memo reactivity.
- **Per-bucket idle timeout stored in `buckets` table, not `app_meta`.** Cleaner than keyed `app_meta` rows; zero migration risk because `migrate_buckets_columns()` already handles idempotent ADD COLUMN.

## Open Questions for Next Session
- Verify the full chain post-relaunch: BucketsList shows [15·30·60] with 60 highlighted; toggle persists; cross-window sync works via buckets://changed.
- Verify Enter / right-Shift-tap flow in a real project window: armed header → Enter → terminal → tap right-Shift → header re-arms.
- After verification, the armed keyboard layer (bucket bar + tab strip + vertical switch + enter/escape flow) can be considered feature-complete for v0.1.

## Notes
- All three features are uncommitted — staged together in the next commit per user's instruction.
- TypeScript and Rust both compile clean (tsc --noEmit passed, cargo tauri build --debug passed).
- The `arm-focus.ts` module is the only new file; everything else is edits to existing files.
- The 3D bucket workspace TerminalsView receives no `idleSuspendMin` prop — falls back to `AUTO_SUSPEND_MIN` constant (20 min). Intentional; 3D workspace has its own lifecycle model.

---

# Handoff — 2026-05-18 [session 4]

## Session Goal
Make the armed-mode keyboard navigation (ShiftRight → footer → ArrowUp/Down → header) robust against two reported failure modes: Shift dead after auto-resume, and stuck-no-focus state when only one terminal tab is open.

## What Got Done

**Fix 1 — Global ShiftRight-tap detector** (replaces per-TerminalPane listener):
- `src/lib/shift-arm.ts` (new): `setupGlobalShiftArm()` installs three window-level capture listeners (keydown, keyup, mousedown). No focus gate. Mousedown during hold taints to prevent Shift+Click arming. Always dispatches `lexical:arm-switch-vertical { target: "footer" }` on clean tap.
- `src/App.tsx`: imports `setupGlobalShiftArm`, calls it at component creation (synchronous), stores cleanup, runs it in `onCleanup`.
- `src/components/TerminalPane.tsx`: removed all shift-arm code — `shiftDownAt`, `shiftTainted`, `TAP_WINDOW_MS`, `terminalHasFocus`, `onShiftKeyDown`, `onShiftKeyUp`, their window listener registration and cleanup. Removed `lastArmedBar` import.

**Fix 2 — armTabs() arms with 1 tab** (removes early-return guard):
- `src/components/TerminalsView.tsx`: removed `total < 2` early-return from `armTabs()`. Header strip now arms unconditionally when requested. ArrowLeft/Right remain no-ops when n=1 (modular arithmetic keeps index in place). Enter and ArrowDown work as expected from the armed header.

## What Was Tried and Abandoned
- None. Both root causes were clear from Phase 1 codebase exploration before any code was written.

## Decisions Made
- **ShiftRight always targets footer (constant, not lastArmedBar).** Previous behavior re-armed whichever bar was last active. User confirmed: Shift should always go to the project switcher (footer), not carry memory of last side.
- **armTabs arms even with 1 tab.** "No point arming with 0 or 1 items" comment was wrong about user intent — even with one tab, the user wants visual confirmation of which side is active, and needs Enter/ArrowDown to work.

## Open Questions for Next Session
- User testing: does Shift fire reliably on fresh window open + after auto-resume? (The global listener should cover both, but only real use confirms.)
- Is `src/lib/arm-focus.ts` (`lastArmedBar` / `setLastArmedBar`) now dead code? The setters are still called by `BucketBar.arm()` and `armTabs()`, but the only reader was the removed TerminalPane listener. If nothing new reads it, it can be deleted in a cleanup pass. Low priority.

## Notes
- TypeScript compiled clean (tsc --noEmit, zero errors) after all edits.
- Build installed via the safe rm -rf + ditto path; prod app at 12:00:02 mtime. Session PID 64892 survived.
- `src/lib/arm-focus.ts` kept as-is — its setters are harmless; the file is not load-bearing but removing it requires confirming zero other readers first.

---

# Handoff — 2026-05-18 [session 5]

## Session Goal
Two UX polish items: (1) make the active terminal tab visually obvious via full project accent fill; (2) add an M-key shortcut in the armed footer to toggle hide-panels globally across all project windows.

## What Got Done

**Feature 1 — Active tab accent fill** (uncommitted, staged for this commit):
- `src/styles/app.css`: `.terminal-tab.active` background changed from `#0e0e10` (invisible against the strip) to `var(--proj-accent, #2d5cc8)` with matching `border-bottom`. Added `.terminal-tab.active .tab-close` in rgba(255,255,255,0.7) so the close button reads on any accent. Added `.terminal-tab.active .editor-tab-dirty { color: #ffffff }` so the dirty dot doesn't vanish on the accent background.

**Feature 2 — M-key panel toggle (cross-window broadcast)** (uncommitted, staged for this commit):
- `src/components/BucketBar.tsx`: new `else if (e.key === "m" || e.key === "M")` branch in `onKey` dispatches `window.CustomEvent("lexical:toggle-panels")`. Footer stays armed after M.
- `src/App.tsx`: added `emit` to Tauri event import. Declared `onTogglePanels` and `unlistenPanelsChanged` slots. In `onMount`: wires window CustomEvent listener that flips local `panelsHidden` and emits `panels://changed` via Tauri; wires Tauri `listen("panels://changed")` receiver that only acts on project windows and short-circuits on state-equality. Cleanup in `onCleanup`.

## What Was Tried and Abandoned
- **Local-only toggle first**: initial plan used only a window CustomEvent (no Tauri broadcast). User redirected mid-plan to make it global. Revised to Tauri emit/listen before any code was written. No code was rolled back.

## Decisions Made
- **M-key always targets panels toggle, not "disarm footer."** Footer stays armed after M so the user can chain M+M or M+ArrowRight without re-arming.
- **Broadcast via `panels://changed` Tauri event.** New event name, not piggybacking on an existing one. Receiver gates on `windowLabel().startsWith("project-")` so main launcher and bucket-3d workspace are unaffected.
- **Title-bar panel button stays local-only.** User only asked M to broadcast; no change to the existing button.

## Open Questions for Next Session
- New windows opened mid-session start at `panelsHidden=false`, not the current bucket state. User can re-press M. If this bites, fix with a "request current state on mount" emit to other windows.
- Is `src/lib/arm-focus.ts` now dead code? Setters still called, but reader was removed in session 4. Low-priority cleanup.

## Notes
- TypeScript compiled clean before build.
- Build installed at 12:29:58; PID 770 survived.
- `panels://changed` payload is a boolean — the new desired state — not a toggle signal. This guarantees convergence even if windows somehow drift out of sync.

---

# Handoff — 2026-05-18 [session 6]

## Session Goal
Ship the "frequent projects" feature: mark a project as starred (★), Space cycles among only starred projects, stars visible in footer and sidebar. Fix all reported bugs through to a working build.

## What Got Done

**Full feature + three bug fixes** (uncommitted — to be committed after checkpoint):

**Schema + backend** (`src-tauri/src/store.rs`, `src-tauri/src/commands.rs`, `src-tauri/src/main.rs`):
- `is_frequent INTEGER NOT NULL DEFAULT 0` column on `bucket_projects` via `migrate_bucket_projects_columns()`.
- `Project` struct gains `pub is_frequent: bool`; `project_from_row` loads it; `load_bucket_projects` SELECT extended.
- `toggle_project_frequent(bucket_id, project_id)` store method flips value, returns new state.
- `toggle_project_frequent` Tauri command registered in `generate_handler!`.

**Frontend types + IPC** (`src/lib/types.ts`, `src/lib/ipc.ts`):
- `is_frequent: boolean` on `Project` interface.
- `toggleProjectFrequent(bucketId, projectId)` IPC wrapper.

**Star rendering** (`src/components/BucketsList.tsx`, `src/styles/app.css`):
- `<span class="bucket-project-star">★</span>` in sidebar project rows when `project.is_frequent`.
- `<span class="bucket-bar-star">★</span>` in footer `bucket-bar-current` when cursor project is starred.
- `.bucket-bar-star` and `.bucket-project-star` CSS with `var(--star-color, #d4a942)`.

**BucketBar.tsx — F key + optimistic cursor + window switch**:
- F key marks/unmarks current project as frequent. Binding moved from Shift+Space after bug 1.
- `localCursor` signal + `currentCursor()` helper — set optimistically on ArrowLeft/Right, reset to null when `props.activeBucket` resource settles (`createEffect`). Fixes F and Space reading stale cursor.
- Space handler: finds next starred project using `currentCursor()`, calls BOTH `requestOpenProject(nextProject.path)` AND `setBucketCursorToProject` in parallel. Before this fix, Space updated the footer but didn't switch the window.
- `requestOpenProject` and `Project` type added to imports.

**Bug fixes in chronological order**:
1. Shift+Space mis-fired on plain Space (e.shiftKey true while right-Shift held) → moved to F key.
2. F toggled wrong project (stale cursor) / Space was no-op (stale cursor guard) → optimistic `localCursor` signal.
3. Space updated footer display but window didn't switch → added `requestOpenProject` call.

Build installed at 13:42:57; PID 42733 survived.

## What Was Tried and Abandoned
- **Shift+Space binding**: initial spec used Shift+Space for starring. Abandoned after user reported that plain Space mis-fired the star because `e.shiftKey` is true on any keypress while right-Shift is physically held (used for arming the footer). Moved to F key.
  - **Lesson**: Any "Shift+X while Shift is also used as an arm gesture" binding is ambiguous. Use a clean key (no modifiers needed) when Shift has a dual role in the same focus context.

## Decisions Made
- **F key for frequent-toggle (not Shift+Space, not Shift+F).** Clean key, no modifier ambiguity. Stays armed — user can chain F+ArrowRight+F to star multiple projects.
- **Optimistic localCursor pattern for all BucketBar key handlers.** Set synchronously on arrow press, reset when createResource refetch settles. Needed because cycleBucket → `buckets://changed` → Solid createResource is async; handlers ran before the DB cursor was reflected in props.
- **Space calls requestOpenProject + setBucketCursorToProject in parallel.** Mirrors BucketsList sidebar-click pattern (lines 339-356). Can't use cycle_bucket (direction-only); must target by project path.

## Open Questions for Next Session
- `src/lib/arm-focus.ts` (`lastArmedBar` / `setLastArmedBar`) may be dead code — setters are called but the only reader was removed in session 4. Low-priority cleanup pass.
- New windows opened mid-session start at `panelsHidden=false`. See session 5 note — acceptable for now.

## Notes
- TypeScript compiled clean (tsc --noEmit, zero errors) before build.
- Build installed at 13:42:57 mtime; session PID 42733 survived.
- All changes remain uncommitted in the working tree — will be committed immediately after this checkpoint.
